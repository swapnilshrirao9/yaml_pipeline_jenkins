
pipeline:
  agent: 
    label: "python2_agent"
  stages:
    - stage: "Run Shell Commands"
      steps:
        - script:
            - sh: "echo Starting shell stage..."
            - sh: "ls -l"
            - sh: "echo Shell stage finished."